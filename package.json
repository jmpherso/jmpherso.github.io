{
  "name": "vite-template-solid",
  "version": "0.0.0",
  "description": "",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "serve": "vite preview",
    "predeploy": "npm run build", // Ensure the site is built before deploying
    "deploy": "gh-pages -d dist"  // Deploy the 'dist' folder (Vite's build output)
  },
  "license": "MIT",
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "typescript": "^5.7.2",
    "vite": "^6.0.0",
    "vite-plugin-solid": "^2.11.6"
  },
  "dependencies": {
    "solid-js": "^1.9.5"
  }
}
